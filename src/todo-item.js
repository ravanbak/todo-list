function createTodoItem(title, desc, dueDate, priority, notes) {
    return {
        title,
        desc,
        dueDate,
        priority,
        notes
    }
}

export { createTodoItem }